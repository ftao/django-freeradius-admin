language: python
python:
  - "2.7"
# command to install dependencies
install: "pip install -r requirements.txt"
# command to run tests
script: DJANGO_SETTINGS_MODULE=djra.dev_settings python manage.py test
